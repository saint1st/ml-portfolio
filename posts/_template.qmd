---
title: "Post Title"
description: "One-sentence summary for cards and SEO."
date: 2025-10-03
categories: [ML, Notes]   # use your own tags
image: images/og-default.png
---

## Motivation
What problem this solves and why.

## Derivation / Math
Inline math like $x^\top y$ and blocks:
$$
\mathcal{L}(\theta) = - \sum_i \log p_\theta(y_i\mid x_i)
$$

## Minimal Implementation (PyTorch)
```python
import torch, torch.nn as nn

class TinyModel(nn.Module):
    def __init__(self, d=128):
        super().__init__()
        self.net = nn.Sequential(nn.Linear(d, 64), nn.ReLU(), nn.Linear(64, 10))
    def forward(self, x): return self.net(x)
